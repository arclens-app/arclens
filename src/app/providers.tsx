"use client"
// useArcSocket disabled — using polling in ArcLayout instead
// The WebSocket to wss://ws.arc-testnet.io doesn't exist yet

export function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}